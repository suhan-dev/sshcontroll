# SSHcontroll Agent Notes

Read `LLM.md` before making distribution, setup, SSH, Codex, monitor, file
transfer, or permission changes.

Keep the app distributable without removing convenience for an existing local
user. Personal runtime settings live outside the repository in Application
Support and must not be committed.
